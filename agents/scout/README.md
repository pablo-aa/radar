# Scout Agent

Shared across all users. Trigger: cron, weekly. Input: none (reads canonical source list from DB). Tools: web_search + web_fetch + bash + custom "save_opportunities" tool. Output: rows in the opportunities table plus the trends table.

Not implemented in this task (placeholder workspace).
